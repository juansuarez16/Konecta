class User {
    constructor(id, nombreUsuario, contrasena, rol, empleadoId) {
      this.id = id;
      this.nombreUsuario = nombreUsuario;
      this.contrasena = contrasena;
      this.rol = rol;
    }
  }
  
  module.exports = User;
  